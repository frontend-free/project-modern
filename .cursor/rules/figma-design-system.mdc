---
description: 使用 Figma MCP 实现设计稿时的设计系统规则。涵盖组件组织、样式约定、设计 token、资源与图标、Figma 转代码流程。
globs: ["apps/**/*.tsx", "apps/**/*.ts", "packages/**/src/**/*.tsx"]
alwaysApply: false
---

# Figma 设计系统规则

本规则用于将 Figma 设计通过 MCP 转化为本项目一致的前端代码，请在所有 Figma 实现任务中遵循。

## 组件与 UI 规范

- **IMPORTANT**：优先使用 **antd** 与 **ProComponents**（Button、Input、Form、Table、Modal、Select、DatePicker、Card、Space、Layout、Menu、Tabs、Message、notification、ProTable、ProForm、ProLayout 等），避免手写等效 HTML 或重复造轮子；仅 antd 无法满足时再用自定义或原生。
- 其次使用 **@fe-free/core** 的组件，用法见 `@fe-free/core/src/README.md`。
- 单文件不宜过重，可适当拆分子组件。

## Figma MCP 集成流程（必做，不可跳过）

1. 先对目标节点调用 **`get_design_context`**，获取结构化设计与代码提示。
2. 若返回过大或被截断，再调用 **`get_metadata`** 拿到节点层级，然后对需要的节点再次 **`get_design_context`**。
3. 调用 **`get_screenshot`** 作为视觉参考。
4. 在同时具备 **get_design_context** 与 **get_screenshot** 结果后，再下载所需资源并开始实现。
5. 将 MCP 输出（多为 React + Tailwind）**按本项目约定改写**：用 antd/ProComponents/@fe-free/core、本项目 token（如 `border-01`、`text-03!`）、路由与资源规范。
6. 实现完成后对照 Figma 截图做 **视觉与行为 1:1 校验**，再标记完成。

### 实现原则

- 将 Figma MCP 输出视为**设计与行为参考**，不是最终代码风格。
- 用本项目的 **Tailwind + @fe-free/core 变量**（如 `border-01`、`text-03!`）替代随意 hex 或未定义类名。
- **优先复用** antd、ProComponents、@fe-free/core 已有组件，不重复造轮子。
- 统一使用本项目的颜色、字体、间距 token 和路由、状态、数据请求方式。
- 目标：与 Figma 设计 **1:1 视觉与行为一致**，并以截图验证。

## 图标与资源

- **IMPORTANT**：svg 图标优先使用 **@fe-free/icons**。不要安装或引入新的图标/素材库；
- 其次再考虑把 Figma MCP 提供的图片/SVG 资源落盘到 `src/assets/`，如 `src/assets/images/`、`src/assets/svgs/`。

## 项目特定约定

- 注意**文本溢出**：适当使用 `truncate`、`line-clamp` 等。
- 代码保持简洁可读，不做过度优化；不确定处明确说明，不猜测。
